public class Driver {
    double rating;
    String workExperience;
    String fullName;
    String car;

    int firstRate = 5;
    int secondRate = 4;
    int thirdRate = 5;
    double pulRate = (firstRate + secondRate + thirdRate);
    double fullRate = (pulRate / 3);
}
